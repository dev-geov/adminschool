<div class="card">
    <div class="card-header bg-default">
        <div><h2>Disciplinas</h2></div>
    </div>
    <div class="card-body">
        <table class="table">
            <thead>
                <tr>
                    <th>Título</th><th>Professor</th>
                </tr>
            </thead>
            <% @disciplines.each do |discipline| %>
                <tr>
                    <td>
                        <%= link_to discipline.title, discipline %>
                    </td>
                    <td><%= discipline.professor.name %></td>
                </tr>
            <% end %>
        </table>
    </div>
    <div class="card-footer">
        <%= link_to 'Adicionar', new_discipline_path, class: "btn btn-primary" %>
    </div>
</div>
